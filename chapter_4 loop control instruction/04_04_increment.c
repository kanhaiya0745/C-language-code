#include <stdio.h>
#include <conio.h>

int main()
{
    int i = 5;
    printf("The value after i ++ increment is i++ is %d\n", ++i);

    // i++;=> phele print kare fir increment kare 
    // ++i; => phele increment kare phir print kare;

    printf("The value after i ++ increment is i++ is %d\n", ++i);

    i+=15; // it is increment i by 15;
    
    printf("The value after i ++ increment is i++ is %d\n", ++i);


    return (0);
}